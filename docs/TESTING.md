# Beta test checklist

Use disposable worlds and Java 21. Back up any world before installing a beta build.

## Automated and build

```sh
export JAVA_HOME=/home/theyeldo/.local/share/jdks/temurin-21
export PATH="$JAVA_HOME/bin:$PATH"
./gradlew clean test
./gradlew clean build
```

The pure suite must assert codecs/migration/malformed isolation, graph operations and eviction, thread planning/outcomes/restart, contradiction determinism and pair cleanup, contamination/profile decay and bounds, presets, advancement resources, and privacy recipients.

## Disposable persistence sequence

1. Start a disposable world with 0.4.1-alpha and establish Stage/familiar data.
2. Stop cleanly, install 0.5.0-beta.2, and verify the legacy file remains untouched.
3. Create room nodes/edges, a habit, Panic Imprint, Audio Residue, active thread, contamination, and profile samples.
4. Stop and restart cleanly; use `memory status`, `rooms`, `threads`, and `validate`.
5. Complete a persisted-context event and verify `Not Forgotten`; confirm no duplicate entities or stale active lock.

## Client/runtime matrix

- Dedicated server reaches `Done` and stops with `stop`.
- Linux client loads resources; integrated and dedicated connections are separate checks.
- English/Russian, classic/slim skins, existing Echoes, nine Original kinds, seven contradictions, presets, and diagnostics.
- Normal/reduced tick rate; Peaceful; Survival/Creative; disposable Hardcore copy.
- Overworld/Nether transfer, logout during event, death/respawn, restart with paused thread.
- Real two-client checks for `shared_echoes=false` and `true`, distinct simultaneous threads, target isolation, and independent disconnect cleanup.

`docs/VERIFICATION.md` is the result ledger. Mocked, source-audited, or automated checks must not be labelled as real-client or two-client results.
